package com.axppress.hundredblessings.di

/*
import android.content.Context
import androidx.room.Room
import com.axppress.hundredblessings.domain.local.AppDatabase
import com.axppress.hundredblessings.domain.local.MoviesDatabase
import com.axppress.hundredblessings.domain.local.converters.StringListConverter

val localModule = org.koin.dsl.module {
    single { createMoviesDatabase(get<Context>()) }
}
*/

/*
private fun createMoviesDatabase(context: Context) =
    Room.databaseBuilder(context, AppDatabase::class.java, "movies_db")
        .addTypeConverter(StringListConverter())
        .build()
        .movies
*/
