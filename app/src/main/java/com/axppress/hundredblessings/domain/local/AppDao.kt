package com.axppress.hundredblessings.domain.local

/*
@Dao
interface AppDao {

    @Query("SELECT * FROM to_watch")
    fun getToWatchMovies(): Flow<List<ToWatchEntity>>

    @Query("SELECT * FROM favorites")
    fun getFavoriteMovies(): Flow<List<FavoriteEntity>>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insertToWatchMovie(toWatchEntity: ToWatchEntity)

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insertFavoriteMovie(favoriteEntity: FavoriteEntity)

    @Query("DELETE FROM to_watch WHERE movie_id = :movieId")
    fun deleteToWatchMovie(movieId: Long)

    @Delete
    fun deleteFavoriteMovie(favoriteEntity: FavoriteEntity)
}
*/
